// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase:{
    apiKey: "AIzaSyDrcLs2H3rSk9efwgMeqs-HIvcupE_SuPA",
    authDomain: "sketchstatt.firebaseapp.com",
    databaseURL: "https://sketchstatt.firebaseio.com",
    projectId: "sketchstatt",
    storageBucket: "sketchstatt.appspot.com",
    messagingSenderId: "876053125500",
    appId: "1:876053125500:web:85c509856411e741212cf9",
    measurementId: "G-77JQHWVSV1"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
